{{!-- Code referenced from Module 14 - Mini Project --}}

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bitter.</title>
  {{!-- Boostrap CSS Only --}}
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="icon" type="image/png" href="/assets/images/angry-emoji.png">
</head>

<body>
    <div class="container-fluid fill">
        <div class="row fill">
            <div class="col-2">
                <div class="container-fluid fill">
                    <div class="c-column fill">
                        <header>
                            <h1 class="mb-5">
                                <a href="/">Bitter. <i class="fa-solid fa-face-angry"></i></a>
                            </h1>
                            <nav class="c-column">
                                <h4 class="column-item-spacing">
                                    <a href="/">
                                        Homepage
                                        <i class="fa-sharp fa-solid fa-house-chimney"></i>
                                    </a>
                                </h4>
                                <h4 class="column-item-spacing">
                                    <a href="/dashboard">
                                        Dashboard
                                        <i class="fa-solid fa-gauge-simple-high"></i>
                                    </a>
                                </h4>
                            </nav>
                        </header>
                        <footer>
                            <!-- Conditionally render sign in or logout links -->
                            {{#if loggedIn}}
                                <button id="logout" class="btn btn-primary column-item-spacing">
                                    Logout
                                </button>
                                {{else}}
                                <a href="/signin">
                                    <button class="btn btn-primary column-item-spacing">
                                        Sign In
                                    </button>
                                </a>
                            {{/if}}
                            <div class="py-3">
                                <h6>Bitter. © 2022</h6>
                            </div>
                        </footer>
                    </div>
                </div>
            </div>
            <div class="col">
                <main class="container-fluid mt-5">
                <!-- Render the sub layout -->
                {{{ body }}}
                </main>

            </div>
        </div>
    </div>

    
    

    <!-- Bootstrap JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
    {{!-- Font Awesome --}}
    <script src="https://kit.fontawesome.com/b3e3f4c117.js" crossorigin="anonymous"></script>
    <!-- Render script for logged in users only -->
    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>
